.team{"data-team-id" => team.id}
  .heading
    %h5
      = link_to(team.name, team_url(team))
  %ul.team-memberships
    = render(partial: "team_membership", collection: team.active_real_memberships)
