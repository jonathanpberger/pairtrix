#addEmployee.modal.hide.fade{tabindex: "-1", role: "dialog", "aria-labelledby" => "addEmployeeLabel", "aria-hidden" => "true"}
  .modal-header
    %button.close{type: "button", "data-dismiss" => "modal", "aria-hidden" => "true"}
      x
    %h3#addEmployeeLabel
      Add Employee
  .modal-body
    - employee = company.employees.build
    = simple_form_for [company, employee], html: {id: "new_employee_ajax"} do |f|
      = f.input :first_name
      = f.input :last_name
      = f.input :avatar, as: :file
      = f.hidden_field :avatar_cache
      = f.input :remote_avatar_url, label: "Or Image URL:"
      = f.input :remove_avatar, as: :boolean, inline_label: "Delete Avatar", label: false
      - if employee.avatar?
        .control-group
          = f.label :avatar_image
          .controls
            = image_tag(employee.avatar_url)

  .modal-footer
    %button.btn{"data-dismiss" => "modal", "aria-hiddeni" => "true"}
      Cancel
    %button#add-employee-submit.btn.btn-primary
      Add Employee
