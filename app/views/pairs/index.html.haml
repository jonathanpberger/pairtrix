%h3 Pairs

= link_to 'New pair', new_pairing_day_pair_path

%table.table.table-striped.table-condensed.table-bordered
  %tr
    %th
      Name
    %th
      Action
  = render(partial: "pair_row", collection: @pairs, as: 'pair')

