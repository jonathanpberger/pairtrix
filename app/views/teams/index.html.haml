= render(partial: "shared/breadcrumb", locals: { company: @company })
%h3
  Teams

.new-item
  = link_to 'New Team', new_company_team_path(@company)

%table.table.table-striped.table-condensed.table-bordered
  %thead
    %tr
      %th
        Name
      %th
        Action
  %tbody
    = render(partial: 'team_row', collection: @teams, as: 'team')
= link_to("Back", company_url(@company))
