= render(partial: "shared/breadcrumb", locals: { company: @team.company, team: @team })
%h3
  #{@team.name} pairing matrix since #{2.weeks.ago.to_date}
%br

= render(partial: 'matrix', locals: { team: @team })

