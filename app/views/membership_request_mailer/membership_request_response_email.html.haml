!!!
%html.no-js{:xmlns => "http://www.w3.org/1999/xhtml", "xml:lang" => "en", :lang => "en"}
  %head
    %meta{ :content => "text/html; charset=utf-8", "http-equiv" => "Content-Type" }
    %title
      #{@status} membership request to #{@company.name} for #{@user.name}

  %body
    %h3
      #{@status} membership request to #{@company.name} for #{@user.name}
    %p
      %strong
        Name:
      = @user.name
    %p
      %strong
        Email:
      = @user.email
    %p
      %strong
        Status:
      = @status
    - if @status == "Approved"
      %p
        Please sign in at #{link_to("http://www.pairtrix.com/", "http://www.pairtrix.com", target: "_blank")}


